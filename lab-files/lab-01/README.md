# L1 配套文件

- `source-paths.json`：允许引用的固定版路径锚点。
- `answer-template.json`：学习者答案模板。
- `examples\good-answer.json`：可通过结构自查的示例。
- `examples\bad-answer.json`：缺少关键节点且含过度声明的负例。
- `self-check.mjs`：公开结构自查，不证明语义完全正确。
